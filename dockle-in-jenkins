pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                sh 'docker build -t myapp:latest .'
            }
        }
        stage('Run Dockle Scan') {
            steps {
                sh 'docker run --rm goodwithtech/dockle myapp:latest'
            }
        }
    }
}
